<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>E-Commerce Admin Header</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9O5SmXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous">
    <style>
        .navbar-admin-brand {
            font-weight: bold;
            font-size: 1.6rem;
            color: #dc3545 !important; /* Red for admin brand */
        }
        .navbar-admin-nav .nav-link {
            color: #6c757d !important; /* Muted grey for admin links */
            margin-right: 15px;
            transition: color 0.3s ease-in-out;
        }
        .navbar-admin-nav .nav-link:hover {
            color: #dc3545 !important; /* Red on hover */
        }
        .admin-btn-logout {
            background-color: #dc3545;
            color: white;
            border-color: #dc3545;
            transition: background-color 0.3s ease-in-out;
        }
        .admin-btn-logout:hover {
            background-color: #c82333; /* Darker red on hover */
            border-color: #bd2130;
        }
        .navbar-admin {
            background-color: #f8d7da; /* Light red background for admin nav */
            border-bottom: 3px solid #dc3545; /* Stronger red border */
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-admin">
        <div class="container-fluid">
            <a class="navbar-admin-brand" href="/">Admin Panel</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNavbar" aria-controls="adminNavbar" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="adminNavbar">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 navbar-admin-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/products">View Products</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/products/add">Add Products</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/orders">View Orders</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/users">View Users</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/complaints">Complaints</a>
                    </li>
                </ul>
                <div class="d-flex">
                    <% if (user) { %>
                        <span class="navbar-text me-3">
                            Welcome, <%= user.username %>!
                        </span>
                        <a class="btn admin-btn-logout" href="/auth/logout">Logout</a>
                    <% } else { %>
                        <a class="btn btn-outline-secondary" href="/admin/login">Admin Login</a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
