<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Login' %> - Fashionista</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/auth/login.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/user_header') %>
    <div class="container-fluid vh-100 p-0">
        <div class="row g-0 h-100">
            <div class="col-lg-7 d-none d-lg-block image-container">
                <div class="branding-overlay">
                    <h1 class="display-4 fw-bold">Style Redefined</h1>
                    <p class="lead">Log in to Fashionista. Your style journey continues.</p>
                </div>
            </div>

            <div class="col-lg-5 col-md-12 form-container d-flex align-items-center justify-content-center">
                <div class="form-box p-4 p-md-5">
                    <div class="text-center mb-3">
                        <h2 class="form-title fw-bold">Welcome Back!</h2>
                        <p class="form-subtitle text-muted">Log in to your account.</p>
                    </div>

                    <form id="loginForm" novalidate>
                        <div class="mb-2">
                            <label for="email" class="form-label">Email Address</label>
                            <input type="email" class="form-control" id="email" name="email" required>
                        </div>

                        <div class="mb-2">
                            <label for="password" class="form-label">Password</label>
                            <input type="password" class="form-control" id="password" name="password" required>
                        </div>

                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <div class="form-check">
                                <input type="checkbox" class="form-check-input" id="showPassword">
                                <label class="form-check-label" for="showPassword">Show Password</label>
                            </div>
                            <div>
                                <a href="#">Forgot Password?</a>
                            </div>
                        </div>

                        <div class="d-grid mb-2">
                            <button type="submit" id="submitButton" class="btn btn-primary btn-lg fw-bold">Log In</button>
                        </div>

                        <div id="formMessage" class="mt-3 text-center"></div>

                        <div class="mt-3 text-center">
                            <p class="text-muted">Don't have an account? <a href="/auth/register">Register</a></p>
                        </div>
                    </form>
                    <button id="googleSignup" onclick="window.location.href='/auth/google/login'" class="btn w-100 d-flex align-items-center justify-content-center" style="border-radius: 50px; border: 1px solid #ccc; background-color: white; color: #000; font-weight: 500;">
                          <img src="https://developers.google.com/identity/images/g-logo.png" alt="Google" width="20" class="me-2">
                        Log in with Google
                      </button>
                </div>
            </div>
        </div>
    </div>
    <%- include('../partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/auth/login.js"></script>
</body>
</html>
